spring:
  profiles:
    active: dev
  application:
    name: trace-service
  datasource:
    type:
#apollo appid 设置
app.id: trace-service

# 禁用nacos的log,解决logback和nacos自带的logback设置设置冲突问题
nacos:
  logging:
    default:
      config:
        enabled: false



